<h1>Index route</h1>

<h2>Questions:</h2>
<ul>
  {{#each model as |question|}}
    <li>{{#link-to 'question' question.id}}<strong>{{question.title}}</strong>{{/link-to}} - {{question.author}}</li>
  {{/each}}
</ul>
